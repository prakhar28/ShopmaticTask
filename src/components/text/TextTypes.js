import Colors from '../../constants/colors'
import { FontSizes, FontWeight } from '../../constants/sizes'

const TextTypes = {
  headingText: {
    color: Colors.black,
    fontSize: FontSizes.xxl,
    fontWeight: FontWeight.bold
  },
  bodyText: {
    color: Colors.black,
    fontSize: FontSizes.md
  }
}

export default TextTypes
